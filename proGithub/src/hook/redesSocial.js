import { userConfig } from '../config/config.js';

const redesSocial = () => {
    return (`
        <div class="icons">
            ${userConfig.redeSocial}
        </div>
    `)
}

export default redesSocial;